package com.top.app.core.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class TopController {
    /**
     * log4j接口
     */
    protected Logger logger = LoggerFactory.getLogger(this.getClass());
}
